import 'package:flutter/material.dart';

/// A central place for all color definitions used in the app.
class AppColors {
  // Example Primary (replace with your actual HEX codes)
  static const Color primarycolor = Color(0xFF6c4baf);
  static const Color primarycolorDark = Color(0xFF463961);

  // Example Secondary (purple)

  static const Color secondary700 = Color(0xFF7733CC);
  static const Color activeButton = Color(0xFF40caae);

  // Example Neutrals
  static const Color neutral50 = Color(0xFFF9F9F9);

  // Example Success

  // Add other color categories if needed...
}
